<%- include('../includes/boiler.ejs') %>
<%- include('../includes/nav.ejs') %>

<style>
  :root{
    --bg: #050505;
    --panel: #0b1220;
    --neon: #EDFF00;
    --neon-soft: #d4ff37;
    --accent: #3b82f6;
    --muted: #9aa4b2;
    --glass: rgba(255,255,255,0.02);
    --glass-strong: rgba(255,255,255,0.04);
  }

  /* page background / atmosphere */
  body{
    background:
      radial-gradient(900px 500px at 8% 10%, rgba(59,130,246,0.08), transparent 14%),
      radial-gradient(700px 420px at 92% 90%, rgba(16,185,129,0.04), transparent 14%),
      var(--bg);
    color:#fff;
    font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    -webkit-font-smoothing:antialiased;
    -moz-osx-font-smoothing:grayscale;
    margin:0;
  }

  /* container */
  .teacher-wrap {
    max-width: 1100px;
    margin: 48px auto;
    padding: 28px;
    box-sizing:border-box;
    display:grid;
    grid-template-columns: 1fr 420px;
    gap:28px;
    align-items:center;
  }

  /* left: intro + card */
  .auth-hero {
    padding: 28px;
    border-radius: 16px;
    background: linear-gradient(180deg, rgba(255,255,255,0.01), rgba(0,0,0,0.45));
    border: 1px solid var(--glass-strong);
    box-shadow: 0 30px 80px rgba(0,0,0,0.7), 0 0 50px rgba(59,130,246,0.03) inset;
  }

  .hero-eyebrow {
    font-size:12px;
    color:var(--neon-soft);
    font-weight:800;
    letter-spacing:.16em;
    text-transform:uppercase;
    display:inline-flex;
    gap:10px;
    align-items:center;
    margin-bottom:12px;
  }

  .hero-eyebrow::before{
    content:"";
    width:34px;height:3px;border-radius:999px;
    background:linear-gradient(90deg, transparent, var(--neon), transparent);
    box-shadow:0 8px 28px rgba(237,255,0,0.06);
    display:inline-block;
  }

  .auth-title {
    font-size:34px;
    font-weight:900;
    line-height:1.04;
    color:#f6f7f8;
    margin:0 0 10px 0;
  }

  .auth-sub {
    color:var(--muted);
    margin-bottom:18px;
    max-width:680px;
    font-size:15px;
    line-height:1.6;
  }

  /* centered card */
  .teacher-auth-wrap {
    max-width:520px;
    margin-top:12px;
    padding: 22px;
    border-radius:14px;
    background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(0,0,0,0.6));
    border: 1px solid rgba(255,255,255,0.04);
    box-shadow: 0 24px 60px rgba(0,0,0,0.7), 0 0 40px rgba(237,255,0,0.03) inset;
  }

  .brand-row {
    display:flex;
    gap:12px;
    align-items:center;
    margin-bottom:12px;
  }

  .brand-dot {
    width:48px;height:48px;border-radius:12px;
    display:flex;align-items:center;justify-content:center;
    background: linear-gradient(135deg,var(--neon-soft),var(--neon));
    color:#081018;font-weight:900;
    box-shadow:0 12px 34px rgba(237,255,0,0.12);
    font-size:18px;
  }

  .teacher-title {
    font-size:20px;
    font-weight:900;
    margin:0;
  }

  .teacher-sub {
    margin:6px 0 14px 0;
    color:var(--muted);
    font-size:14px;
  }

  label{
    display:block;
    color:#eaeaea;
    font-weight:700;
    font-size:13px;
    margin-bottom:6px;
  }

  input[type="text"],
  input[type="password"],
  input[type="email"]{
    width:100%;
    padding:12px 14px;
    border-radius:12px;
    border:1px solid rgba(255,255,255,0.04);
    background: rgba(255,255,255,0.02);
    color:#fff;
    box-sizing:border-box;
    margin-bottom:12px;
    outline:none;
    transition: box-shadow .12s ease, transform .08s ease, border-color .12s ease;
  }

  input:focus {
    border-color: rgba(59,130,246,0.7);
    box-shadow: 0 10px 34px rgba(59,130,246,0.08);
    transform: translateY(-1px);
  }

  .btn-submit {
    width:100%;
    padding:12px 14px;
    border-radius:999px;
    border:none;
    background: linear-gradient(135deg,var(--neon),var(--neon-soft));
    color:#051018;
    font-weight:900;
    cursor:pointer;
    box-shadow: 0 16px 44px rgba(237,255,0,0.28), 0 0 28px rgba(59,130,246,0.06);
    transition: transform .12s ease, box-shadow .12s ease;
    display:inline-flex;
    gap:10px;
    align-items:center;
    justify-content:center;
    font-size:15px;
  }

  .btn-submit:hover { transform: translateY(-2px); box-shadow: 0 20px 54px rgba(237,255,0,0.36); }

  .hint{ color:var(--muted); font-size:13px; text-align:center; margin-top:10px; }

  /* right column - illustration / quick info */
  .auth-side {
    display:flex;
    flex-direction:column;
    gap:18px;
    align-items:center;
    justify-content:center;
  }

  .preview-card {
    width:100%;
    max-width:420px;
    border-radius:18px;
    overflow:hidden;
    background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(0,0,0,0.5));
    border:1px solid rgba(255,255,255,0.04);
    box-shadow: 0 26px 80px rgba(3,8,18,0.8);
    position:relative;
  }

  .preview-card img {
    width:100%;
    height: 360px;
    object-fit:cover;
    display:block;
  }

  .preview-badge {
    position:absolute;
    top:14px;
    left:14px;
    padding:8px 12px;
    background: linear-gradient(135deg,var(--accent),var(--accent-2));
    color:#021018;
    font-weight:800;
    border-radius:999px;
    box-shadow:0 12px 30px rgba(59,130,246,0.12);
    font-size:12px;
  }

  /* bottom neon helper */
  .helper-strip {
    margin-top:16px;
    display:flex;
    gap:8px;
    flex-wrap:wrap;
    justify-content:center;
  }

  .chip {
    padding:8px 14px;
    border-radius:999px;
    background: linear-gradient(135deg, rgba(237,255,0,0.06), rgba(255,255,255,0.01));
    border:1px solid rgba(255,255,255,0.03);
    color:var(--neon-soft);
    font-weight:800;
    font-size:12px;
    box-shadow:0 10px 30px rgba(0,0,0,0.6);
  }

  /* responsive */
  @media (max-width: 980px) {
    .teacher-wrap { grid-template-columns: 1fr; padding:20px; gap:18px; }
    .preview-card img { height:240px; }
  }

  @media (max-width: 520px) {
    .teacher-wrap { margin:24px 14px; }
    .teacher-auth-wrap { padding:18px; }
    .brand-dot { width:40px;height:40px;font-size:16px; }
    .auth-title { font-size:26px; }
  }
</style>

<main class="teacher-wrap" role="main" aria-labelledby="teacherAccess">
  <section class="auth-hero" aria-hidden="false">
    <div class="hero-eyebrow">Welcome to UniformCheck Pro</div>
    <h1 id="teacherAccess" class="auth-title">Teacher Access — Secure Detection Console</h1>
    <p class="auth-sub">
      Staff login to access the live detection console and manage student compliance. Use the Teacher ID and password provided by your administrator.
    </p>

    <div class="teacher-auth-wrap" role="form" aria-labelledby="teacherAccess">
      <div class="brand-row">
        <div class="brand-dot" aria-hidden="true">UC</div>
        <div>
          <div class="teacher-title">Teacher Access</div>
          <div class="teacher-sub">Enter credentials to open the detection dashboard.</div>
        </div>
      </div>

      <form action="/teacher-login" method="POST" autocomplete="off" novalidate>
        <label for="teacherId">Teacher ID</label>
        <input type="text" id="teacherId" name="teacherId" required placeholder="e.g. TCHR-12345" aria-required="true">

        <label for="password">Password</label>
        <input type="password" id="password" name="password" required placeholder="Enter password" aria-required="true">

        <button type="submit" class="btn-submit" aria-label="Login as teacher">
          <i class="fa-solid fa-right-to-bracket" aria-hidden="true" style="font-size:14px;"></i>
          Login as Teacher
        </button>
      </form>

      <p class="hint">This page is for staff only. Keep your credentials confidential.</p>
    </div>

    <div class="helper-strip" aria-hidden="true">
      <div class="chip">Live Detection</div>
      <div class="chip">Real-time Alerts</div>
      <div class="chip">Secure & Auditable</div>
    </div>
  </section>

  <aside class="auth-side" aria-hidden="true">
    <div class="preview-card" role="img" aria-label="Preview illustration">
      <div class="preview-badge">LIVE</div>
      <!-- use your site image here -->
      <img src="/home-1.png" alt="Preview of Student Verification" loading="lazy">
    </div>

    <div style="text-align:center; color:var(--muted); font-size:13px; max-width:420px;">
      Need help? Ask your admin for the Teacher ID. For security reasons, avoid sharing credentials over email.
    </div>
  </aside>
</main>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />

<%- include('../includes/footer.ejs') %>
